const should = require('should') // eslint-disable-line
should.config.checkProtoEql = false
const StatisticsFieldsBuilder = require('../../../../lib/helpers/fields/statistics-fields-builder')

describe('StatisticsFieldsBuilder', () => {
})
